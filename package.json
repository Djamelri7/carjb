{
  "name": "carcare",
  "version": "1.0.0",
  "private": true,
  "description": "CarCare — Car Maintenance Tracker",
  "type": "module",
  "scripts": {
    "cap:init": "cap init",
    "cap:sync": "cap sync",
    "cap:copy": "cap copy",
    "cap:add:android": "cap add android",
    "cap:open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
